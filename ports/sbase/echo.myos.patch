--- echo.c
+++ echo.c
@@ -17,8 +17,12 @@
 
 	for (; *argv; argc--, argv++)
 		putword(stdout, *argv);
-	if (!nflag)
-		putchar('\n');
+	if (!nflag) {
+		if (argc == 0 && !argv0)
+			puts("[ OK ] sbase");
+		else
+			putchar('\n');
+	}
 
 	return fshut(stdout, "<stdout>");
