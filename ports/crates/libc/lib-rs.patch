--- a/src/lib.rs
+++ b/src/lib.rs
@@ -303,6 +303,14 @@ cfg_if! {
         pub use crate::xous::*;
 
         prelude!();
+    } else if #[cfg(target_os = "myos")] {
+        mod primitives;
+        pub use crate::primitives::*;
+
+        mod myos;
+        pub use crate::myos::*;
+
+        prelude!();
     } else {
         // non-supported targets: empty...
     }
