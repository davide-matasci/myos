# myos default lynx.cfg (packed as /etc/lynx.cfg).
# Lynx exits if this file is missing; keep it minimal and explicit.

STARTFILE:https://example.com/
SSL_CERT_FILE:/lib/cacert.pem
CHARACTER_SET:utf-8
PREFERRED_LANGUAGE:en
DEFAULT_CHARSET:utf-8
