/* Minimal cfg_defs.h for myos (skips host cfg_defs.sh / config.cache). */
#ifndef CFG_DEFS_H
#define CFG_DEFS_H 1
static const char *config_cache[] = {
    "myos hand-config (ports/lynx/lynx_cfg.h)",
    (const char *)0
};
#endif
