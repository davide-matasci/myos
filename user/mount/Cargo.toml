[workspace]

[package]
name = "mount"
version = "0.1.0"
edition = "2024"
publish = false
description = "Userspace mount: no args prints /proc/mounts; else mount src tgt fstype"

[[bin]]
name = "mount"
test = false
bench = false

[dependencies]
myos-user = { path = "../lib" }

[profile.dev]
panic = "abort"
opt-level = "s"
debug = false
strip = "debuginfo"

[profile.release]
panic = "abort"
opt-level = "s"
debug = false
strip = "debuginfo"
