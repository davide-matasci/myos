--- dirname.c
+++ dirname.c
@@ -1,6 +1,6 @@
 /* See LICENSE file for copyright and license details. */
 #include <libgen.h>
-#include <stdio.h>
+#include <string.h>
 
 #include "util.h"
 
@@ -21,7 +21,10 @@
 	if (argc != 1)
 		usage();
 
-	puts(dirname(argv[0]));
-
-	return fshut(stdout, "<stdout>");
+	{
+		char *p = dirname(argv[0]);
+		writeall(1, p, strlen(p));
+		writeall(1, "\n", 1);
+	}
+	return 0;
 }
